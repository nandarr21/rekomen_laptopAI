[variables]
PYTHON_VERSION = "3.11"

[phases.build]
cmds = ["pip install -r requirements.txt", "python train_model.py"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"